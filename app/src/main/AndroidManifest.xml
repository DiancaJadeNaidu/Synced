<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Synced"
        tools:targetApi="34"
        tools:ignore="ExtraText">

        <!-- Main launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- User auth & onboarding -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".RulesActivity" />
        <activity android:name=".QuestionnaireActivity" />
        <activity
            android:name=".ChoosingIntentActivity"
            android:theme="@style/Theme.Synced.Fullscreen"/>

        <!-- Matchmaking & profiles -->
        <activity android:name=".TopMatchesActivity" />
        <activity android:name=".ViewProfileActivity" />
        <activity android:name=".SendMessageRequestActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".SettingsActivity" />

        <!-- Sync / friend requests -->
        <activity android:name=".SyncRequestsActivity" />
        <activity android:name=".SyncedFriendsActivity" />
        <activity android:name=".FriendGamesActivity" />


    </application>
</manifest>
